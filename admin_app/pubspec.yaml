name: cma_admin_portal
description: CMA MCQ Portal Admin App
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.0.3
  excel: ^4.0.6
  pdf: ^3.11.1
  path_provider: ^2.1.5
  share_plus: ^12.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
